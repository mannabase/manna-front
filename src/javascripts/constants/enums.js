const providerState = Object.freeze({
    NO_PROVIDER: 'no provider',
    HAS_PROVIDER: 'has provider'
})

const providers = Object.freeze({
    METAMASK: "metamask"
})

export default {providerState, providers}